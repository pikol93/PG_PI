import 'package:flutter/material.dart';

class TracksRoute extends StatelessWidget {
  const TracksRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("tracks route"),
    );
  }
}